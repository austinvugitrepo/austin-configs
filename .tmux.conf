bind -r C-j resizep -D
bind -r C-k resizep -U
bind -r C-h resizep -L
bind -r C-l resizep -R
bind j selectp -D
bind k selectp -U
bind h selectp -L
bind l selectp -R
 
#rebinding default l keybinding to Right:
bind Right last 

set -g mode-keys vi
set -g status-keys vi
